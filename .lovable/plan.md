# מנויי כבישי אגרה ופנגו — רכבים פרטיים של עובדים

## התמונה הכוללת

רוב הרכבים הרלוונטיים הם רכבים **פרטיים של העובדים** ולכן הם לא פריטים ב"משאבים" (אין להם תיק פריט, אין מסירה, אין תוקף טסט בניהול החברה). לעומת זאת המנוי עצמו (כביש 6, פנגו) הוא **התחייבות של החברה** ולכן חייב להופיע ריכוזית במסך משאבים.

לכן:

```text
תיק עובד ← טאב "רכב ומנויים"   = הזנת פרטי הרכב הפרטי + המנויים שלו
משאבים   ← מסך "מנויים"         = טבלה ריכוזית של כל המנויים בחברה (פרטי + חברה)
```

שני המסכים קוראים וכותבים לאותם נתונים — סירקולציה מלאה, בלי כפילות הזנה.

## 1. מודל הנתונים

טבלה חדשה `employee_vehicles` — רכב פרטי של עובד:
- שיוך לעובד וחברה, מס' רכב פרטי, יצרן/דגם, שנה, צבע, בעלות (פרטי / ליסינג פרטי), פעיל/לא פעיל, הערות.

טבלה חדשה `vehicle_subscriptions` — מנוי לרכב:
- שיוך לרכב: או `employee_vehicle_id` (רכב פרטי) או `asset_id` (רכב חברה קיים במשאבים) — בדיוק אחד מהם.
- סוג מנוי: כבישי אגרה (כביש 6 / נתיבי איילון), פנגו, סלופארק, מנהרות הכרמל, אחר.
- מספר מנוי / מזהה חיצוני, ספק, תאריך התחלה, תאריך סיום/חידוש, סטטוס (פעיל / מושהה / בוטל), עלות חודשית, מי מחויב (חברה / עובד / השתתפות), הערות.
- שיוך העובד נגזר אוטומטית מהרכב, כך שהחלפת רכב או סיום העסקה משתקפים מיד.

הרשאות: צפייה ועריכה לתפעול, מנהל מערכת, סופר-אדמין, כספים וחשבות שכר; העובד עצמו רואה את הרכב והמנויים שלו בלבד (קריאה).

## 2. תיק עובד — טאב חדש "רכב ומנויים"

- כרטיס רכב פרטי: הוספה/עריכה/השבתה של רכב (אפשר יותר מרכב אחד).
- מתחת לכל רכב: רשימת המנויים שלו עם סטטוס, מספר מנוי ותאריך חידוש, וכפתור "הוסף מנוי".
- רכבי חברה שכבר משויכים לעובד במשאבים יוצגו באותו טאב לקריאה, עם המנויים שלהם — כך שהעובד/מנהל רואה תמונה אחת.

## 3. משאבים — מסך "מנויים"

- כפתור/כרטיס חדש במסך משאבים הראשי: "מנויים ואגרות".
- טבלה ריכוזית: עובד, לוחית רישוי, סוג רכב (פרטי / חברה), סוג מנוי, ספק, מספר מנוי, סטטוס, תאריך חידוש, עלות חודשית, מי מחויב.
- סינון לפי סוג מנוי, סטטוס, מחלקה, מחויב, ומיון לפי תאריך חידוש; חיפוש חופשי לפי שם עובד או לוחית רישוי.
- שמירת הסינון בין מסכים באמצעות המנגנון הקיים בפרויקט.
- ייצוא לאקסל של הטבלה המסוננת (לחשבות שכר / כספים).
- לחיצה על שורה פותחת את תיק העובד בטאב "רכב ומנויים".
- כרטיסי סיכום למעלה: סה"כ מנויים פעילים, עלות חודשית מצטברת, מנויים לחידוש ב-30 יום, מנויים ללא רכב פעיל.

## 4. סירקולציה והתראות

- סיום העסקה / העברת עובד לסטטוס עוזב → המנויים הפעילים שלו מסומנים כ"דורש סגירה" ומופיעים בצ'קליסט העזיבה כפריט לתפעול.
- מנוי שתאריך החידוש שלו מתקרב מוצג בהתראות התוקפים הקיימות באותה שפה של שאר הפקיעות במערכת.

## פרטים טכניים

- מיגרציה: `employee_vehicles`, `vehicle_subscriptions` (כולל GRANT, RLS, טריגר `updated_at`, ואילוץ שרכב פרטי או פריט-חברה — לא שניהם).
- קבצים חדשים: `src/hooks/useVehicleSubscriptions.ts`, `src/components/vehicles/EmployeeVehiclesTab.tsx`, `src/components/vehicles/VehicleSubscriptionDialog.tsx`, `src/pages/VehicleSubscriptions.tsx`.
- קבצים שישתנו: `src/pages/EmployeeDetail.tsx` (טאב חדש), `src/pages/Assets.tsx` (כניסה למסך המנויים), `src/App.tsx` (נתיב `/assets-subscriptions`), `src/lib/exportExcel.ts` (ייצוא), ושילוב פריט העזיבה ב-`create_offboarding_checklist`.
- ללא שינוי במבנה הדומיין/קטגוריה/תת-קטגוריה הקיים — מנויים הם ישות נפרדת מרכבי החברה, אבל מקושרים אליהם כשצריך.
